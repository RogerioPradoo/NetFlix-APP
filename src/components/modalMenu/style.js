import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    Barra: {
        backgroundColor: '#000',
        flexDirection: 'column',
        width: '40%',
        flex: 1
    },
    textBarra: {
        color: "#FFF", marginBottom: 25, fontSize: 20
    }
});

export default styles